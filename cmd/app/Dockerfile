# Используйте официальный образ Golang в качестве базового образа
FROM golang:latest

# Установите рабочую директорию внутри контейнера
WORKDIR /app

# Скопируйте содержимое текущего каталога внутрь контейнера
COPY . .

# Соберите ваше приложение
RUN go build -o main ./cmd/app

# Установите переменную окружения для вашего приложения
# ENV APP_ENV=production

# Укажите порт, который ваше приложение будет слушать
EXPOSE 8080

# Команда для запуска вашего приложения
CMD ["./main"]

# на будущее посмотреть сюда https://github.com/evrone/go-clean-template/blob/master/Dockerfile
# если это не для старой версии